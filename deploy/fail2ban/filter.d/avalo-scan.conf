# Avalo — 偵測漏洞掃描／表單灌水：同一 IP 在短時間內大量觸發 4xx。
# 對應 2026-07-30 事件（單一 IP 7 分鐘內灌 /api/contact 205 次、全站 8000+ 次請求）。
# 444 是 nginx.conf 對掃描路徑直接斷線的回應碼，一併計入。
[Definition]
failregex = ^<HOST> -[^"]*"[A-Z]+ [^"]*" (?:400|403|404|405|429|444) \d+
ignoreregex =
datepattern = ^[^\[]*\[({DATE})
